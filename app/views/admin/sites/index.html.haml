%h1 Listing sites

= link_to 'New site', new_admin_site_url, class: 'btn btn-default'

%table.table
  %thead
    %tr
      %th name
      %th fqdn
      %th
      %th

  %tbody
    - @sites.each do |site|
      %tr
        %td= site.name
        %td= site.fqdn
        %td= link_to 'Show', admin_site_url(site)
        %td= link_to 'Edit', edit_admin_site_url(site)
