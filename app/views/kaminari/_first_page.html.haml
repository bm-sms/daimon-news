- text = capture do
  %span.pagination__text
    = raw(t 'views.pagination.first')

%p.pagination__item= link_to_unless current_page.first?, text, url, remote: remote, class: 'pagination__link pagination__link--first', data: defined?(data) ? data : nil
