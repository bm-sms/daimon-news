= bootstrap_form_for [:editor, @serial], layout: :horizontal, label_col: 'col-sm-2', control_col: 'col-sm-10' do |f|
  = f.text_field :title
  = f.text_area :description
  = f.text_field :slug, help: "URLの一部に使うこの連載を表す英単語を指定します"

  = f.form_group do
    = f.submit
