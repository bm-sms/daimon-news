- if pickup_posts.exists?
  %h2.subheading.subheading--aside.subheading--press-articles.subheading--press-articles--pickup ピックアップ記事
  %ul
    - pickup_posts.includes(:post).ordered.each do |pickup_post|
      = render partial: 'posts/summary_pickup', locals: {post: pickup_post.post}
